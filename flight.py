class Flight():
        
    def __init__(self, flightNumber,origin,destination, distance):
        
        self.flightNumber = []       
        self.origin = origin
        self.destination = destination
        self.distance = distance
        self.flightNumberList = flightNumberList
        selfself.flightAirplaneType = flightAirplaneType

    def origin(self):
        
        code= self.origin

    def destination(self):
        
        code = self.destination
        
    def isInternational(self):
        
        return self.origin.code != self.destination.code
                      
    def addFlightNumber(self):
        
        flightNumberList += flightNumber

    def flightAirplaneType(self):
        
        self.flightAirplaneType.setPlane()
        
    def overBooked(self, seats):
        
        if len(self.passengerList.addPassenger()) > self.seats:
            return "overbooked by" + len(self.passengerList.addPassenger()) - self.seats
        else:
            return 0
