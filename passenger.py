

class Passenger():
    
    def __init__(self, name, passportNumber):

        self.name = name
        self.passportNumber = passportNumber
        passengersList = list()
        noPassportsList = list()
        self.passengerPoints = passengerPoints                    

    def noPassports(self):
        
        if self.passportNumber == 0:
            noPassportsList.append(self.name)
            
    def origin(self):
        
        code= self.origin

    def destination(self):
        
        code = self.destination

    def calculatePoints(self):
        
        if isInternational():
            self.passengerPoints * 3
        else:
            self.passengerPoints * 1

    def addPassenger(self, name ):
        
        self.passengerList.append(self.name)
        self.passengerList.addFlightNumber()
